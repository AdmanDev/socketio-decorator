/**
 * Defines the options for the listener decorators
 */
export type ListenerDecoratorOptions = {
	/**
	 * If true, the data validation will be disabled for this handler
	 */
	disableDataValidation?: boolean
}
